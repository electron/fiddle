// Defined by Forge's Vite plugin while `electron-forge start` serves the
// renderer; absent from every build. Read it only through `typeof`.
declare const MAIN_WINDOW_VITE_DEV_SERVER_URL: string | undefined;
